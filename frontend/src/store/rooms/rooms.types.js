const FETCH_ROOMS = 'fetchRooms';
const CREATE_ROOM = 'createRoom';
const UPDATE_ROOM = 'updateRoom';

const SET_ROOMS = 'setRooms';

export const actionTypes = {
  FETCH_ROOMS,
  CREATE_ROOM,
  UPDATE_ROOM,
};

export const mutationTypes = {
  SET_ROOMS,
};
