const FETCH_BOOKING_TO_CANCEL = 'fetchBookingToCancel';
const SET_BOOKING_TO_CANCEL = 'bookingToCancel';
const REMOVE_CANCELLED_BOOKING = 'removeCancelledBooking';
const CANCEL_BOOKING = 'cancelBooking';

const actions = {
  FETCH_BOOKING_TO_CANCEL,
  CANCEL_BOOKING,
};

const mutations = {
  SET_BOOKING_TO_CANCEL,
  REMOVE_CANCELLED_BOOKING,
};

export default {
  actions,
  mutations,
};
